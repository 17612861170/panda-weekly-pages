# 熊猫密室周报标准方案

## 1. 母版与变更边界

- 母版链接：<https://17612861170.github.io/panda-weekly-pages/reports/20260803-20260809/>
- 母版基线：以 2026/08/13 刷新后线上显示的最新版本为准（Git 提交 `6302577`）。
- 2026/08/03-2026/08/09 周报是后续周报唯一视觉、排版与交互母版；后续复制母版时必须从上述固定链接对应版本生成，不得从旧 HTML、旧截图或历史缓存恢复。
- 每周只新建一个独立固定链接；同一周的所有更新继续发布到该周固定链接，不另发 HTML 或临时链接。
- 每一个固定链接都必须支持多人同步填写、自动保存、相互可见；没有通过云端同步验收的链接，不算发布完成。
- 未收到赵海霞明确的排版修改指令时，只更新数据、日期、排名、经营提问及承接内容，不改变页面结构、板块顺序、列宽、颜色、控件和交互。
- 修改必须严格限定在用户点名的区域，不顺手重构，不删除城市经理已填写内容。

## 2. 固定链接协同填写红线

- 每周一个固定链接：新周报创建新目录和新链接；同一周后续所有修改只更新该周固定链接。
- 固定链接不是静态展示页，必须接入原有 Supabase 云端表，支持城市经理、店长多人同时填写。
- 页面内所有可填写内容必须自动保存到云端；刷新页面、换浏览器、换设备后内容不能丢失。
- 任意一人填写后，其他人打开同一固定链接必须能看到最新内容；允许短暂同步延迟，但不能要求重新发 HTML。
- 发布前必须用至少两个浏览器或两个会话验证：填写、云端保存提示、另一个会话可见、刷新后保留。
- 若页面显示“云端保存失败”“已暂存在本机”或多人不可见，必须停止交付，先修复同步，不得把链接发给城市经理。
- 生成下一周周报时，上一周城市经理填写的“本周计划”必须完整迁移为新周报的“上周承诺完成（回顾）”，不得删减。

## 3. 数据来源与优先级

1. 用户明确指定的最新截图为对应指标的最高优先级原始数据。
2. 小程序截图优先覆盖：营收、目标、完成率、办卡、续卡、卡型、金额类、排名类、人员排名类；这些字段不得用飞书、旧页面或历史数据替代。
3. 用户明确提供 Excel/文档并指定“以该文档为准”时，该文档优先覆盖对应门店和对应字段。
4. 飞书“门店数据汇总-周”等原始表仅用于截图未包含、且用户明确指出来源的指标，例如去重老客数、新客数。
5. 同一指标存在多版数据时，以用户最后明确确认的截图、文档或列为准。
6. 禁止根据比例、营收或其他字段反推截图中已有的原始值；禁止用估算值、旧周数据或相邻指标填空。
7. 截图未提供且飞书原表无法核实的数据应标记待核，不得编造。发布前必须向用户说明缺失项，不能悄悄留空或写 0。
8. 武汉荟聚等用户单独指定过修订截图的门店，必须采用该门店最新专属截图，不得被批量数据覆盖。

## 4. 时间口径

- 本周：当期周一至周日的真实周汇总。
- 上周：前一周周一至周日的真实周汇总。
- 本月：当月 1 日至当前数据截止日的真实月至今汇总，不得复用本周数据。
- 自定义：仅展示所选日期范围内真实可用的数据；没有逐日数据时明确提示不可用，不得用周汇总冒充。
- 数据可用起始日：2026/07/20。
- 品牌、区域、各区域门店排名支持本周、上周、本月、自定义；员工、店长、区域店员排名在没有逐人数据时，本月和自定义保持暗色禁用。
- 所有动态表头必须写明周期，例如“本月新客数”“本周营收”，禁止只写“新客数”造成跨周期误比。
- 核验必须同周期比较：品牌本周新客数应等于三个区域本周新客数之和；月至今只能与月至今比较。

## 5. 核心指标及公式

- 目标差额 = 营收 - 目标金额。
- 新签中大卡率 = 新签中卡率 + 新签大卡率。
- 总引流金额 = 扫楼金额 + 扶梯口金额（以源数据字段定义为准）。
- 总引流占比 = 总引流金额 / 新签营收；品牌、区域、门店全部适用，禁止除以总营收。
- 扫楼占比 = 扫楼金额 / 新签营收，不得除以总营收。
- 续卡占比 = 续卡金额 / 总营收，若源表另有已确认口径，以确认口径为准。
- 表格合计行：金额和人数直接求和；比率和占比优先按汇总分子/汇总分母重新计算，不能简单平均。只有缺少分子分母时才展示平均值，并标注口径。
- 变化单位统一使用 `%`，不显示“百分点”。
- 新签小卡率为负向指标：较上周上涨代表变差，标红；较上周下降代表改善，标绿。

## 6. 页面与排名规范

- 品牌、区域、门店四周关键指标保持同一排版和指标体系，包含营收、完成率、新签办卡、新签中大卡、整体大卡、续卡和引流等关键板块。
- 排名筛选指标顺序：营收、完成率、新签办卡率、续卡金额、续卡占比、续卡率、新签小卡率、新签中卡率/中大卡率、总引流金额、总引流占比。
- 点击哪个指标，就必须立即按该指标重排，前三名显示金、银、铜标记。
- 排名表保留新客数、老客数；门店和区域名称列冻结；数字居中；列宽紧凑但不挤压文字。
- 区域排名标题按“X区门店关键指标排名”命名。
- 门店分层板块放在区域整体页面最下方。
- 异常分布不得用“其他”兜底，必须列出真实指标名称；只展示完成率、新签中大卡率、总引流占比、扫楼占比、续卡占比、新签办卡率。
- 未达标单元格使用粉色背景，品牌、区域、门店执行同一标准。严禁把未达标完成率写成“已达标”。

## 7. 门店会议闭环

- 门店页固定顺序：上周承诺完成（回顾）→ 本周问题 → 本周计划。
- “本周问题”由数据异常自动生成经营提问，要求具体、短、可回答，聚焦时间、渠道、影响金额、原因和纠偏动作，不写泛泛废话。
- 城市经理和店长可手动修改问题、回答与计划，所有填写通过原有 Supabase 云端表实时同步，任何人打开同一固定链接都能看到最新内容。
- 本周计划在生成下周周报时，自动迁移为下周的“上周承诺完成（回顾）”，原文完整保留，不删减城市经理填写内容。
- 行动项数量不限，可新增、删除；状态包括未开始、进行中、已完成；支持上周、本周、自定义时间筛选。
- 现场问题包括门店数据问题和店长现场提报问题，字段至少包含问题描述、解决方法、是否解决，均可手动填写。

## 8. 每周生产流程

1. 复制母版到新的周日期目录，并创建该周唯一固定链接。
2. 接入该周独立 `reportId`，并确认所有可填写字段绑定 Supabase 云端保存。
3. 建立数据清单：品牌、3 个区域、全部门店、店长、员工、区域店员。
4. 按截图逐张录入，记录每张截图对应的周次、区域、门店和指标，禁止跨周覆盖。
5. 从飞书补充截图没有但已确认来源的字段，尤其是去重老客数。
6. 迁移上周“本周计划”为本周“上周承诺完成（回顾）”，保留云端填写内容。
7. 重新计算允许计算的派生指标、合计、排名、异常色和经营提问。
8. 只替换本周数据和内容，保持母版 DOM 结构、样式和交互不变。
9. 发布到该周固定链接，不提供本地 HTML 给用户。
10. 发布后立刻完成云端协同填写验收；未通过时不得交付链接。

## 9. 发布前强制核验

- 品牌人数 = 3 个区域人数合计；区域人数 = 该区域门店合计，且时间口径一致。
- 品牌金额 = 3 个区域金额合计；区域金额 = 门店金额合计。
- 新客数、老客数、营收、续卡金额、总引流金额逐级对账。
- 本周、上周、本月、自定义切换后，数据、排名和表头同步变化。
- 点击每个排名指标，排序结果与原始值逐行一致。
- 所有公式重新验算，尤其是总引流占比和扫楼占比的分母。
- 未达标项全部粉色；达标判断与阈值一致。
- 新客数和老客数不能出现无依据的 0、空白或“待核”。
- 武汉荟聚等单独修订门店与最新截图逐项一致。
- Supabase 编辑、自动保存、跨设备同步、刷新后保留、多人可见全部正常。
- 所有城市经理填写区必须有保存状态反馈；没有云端成功提示时不得交付。
- 桌面端和手机端检查无重叠、无截断、表头颜色一致、冻结列正常。
- 发布后打开线上固定链接核验版本，确认不是缓存或旧链接。

## 10. 交付规则

- 对用户只交付该周固定链接和必要的核验结论，不发送 HTML；未完成云端同步验收不得说“已完成”。
- 若发现源数据冲突，先停在数据层核对，不用排版修改掩盖数据问题。
- 每次修正后复查用户最后一条指令，确保没有修改未授权内容。
- 以上规范默认为强制执行，只有赵海霞明确提出修改时才调整母版或口径。
